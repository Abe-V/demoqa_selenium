# Backlog

## Web Table — `add_new_person(count=None)`

**Файлы:** [`pages/elements_page.py`](pages/elements_page.py) (`WebTablePage.add_new_person`), [`tests/elements_test.py`](tests/elements_test.py) (`TestWebTable`)

**Проблема:** Сейчас при `count > 1` метод возвращает данные только **последней** добавленной персоны. Тесты (`add`, `search`, `update`, `delete`, pagination) проверяют по сути одну запись.

**Риск:** Если на demoqa при нескольких submit сохраняется только последний человек (или предыдущие перезаписываются), текущие тесты это **не поймают**.

**Задача:**

1. Переписать `add_new_person`, чтобы возвращать **список всех** добавленных людей (например `list[list[str]]` — по 6 полей на каждого).
2. Добавить проверку, что **каждая** добавленная персона есть в таблице (по email или полному набору полей), не только последняя.
3. Обновить/расширить тесты `TestWebTable`, чтобы сценарий с `count=3..5` явно ассертил количество строк и совпадение всех записей.

**Ветка (предложение):** `fix/web-table-verify-all-added`
